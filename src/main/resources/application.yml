flyway:
  datasources:
    default:
      enabled: true
datasources:
  default:
    db-type: postgres
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/financeOverview
    username: postgres
    password: password
micronaut:
  application:
    name: financeOverview
jpa:
  default:
    entity-scan:
      packages: com.acrlights.finance_overview.persistence.entities
#r2dbc:
#  datasources:
#    default:
#      dialect: POSTGRES
#      db-type: postgres